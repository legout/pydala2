[project]
name = "pydala"
version = "0.1.0"
description = "poor man´s datalake"
authors = [
    {name = "Volker Lorrmann", email = "ligno.blades@gmail.com"},
]
dependencies = [
    "pyarrow>=12.0.1",
    "s3fs>=2023.6.0",
    "polars>=0.18.11",
    "duckdb>=0.8.1",
    "tqdm>=4.65.0",
]
requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"
